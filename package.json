{
  "name": "kinosayt",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "packageManager": "bun@1.3.13",
  "scripts": {
    "dev": "bunx turbo run dev",
    "build": "bunx turbo run build",
    "lint": "bunx turbo run lint",
    "db:generate": "cd packages/database && bun x prisma generate",
    "db:migrate": "cd packages/database && bun x prisma migrate dev",
    "db:seed": "cd packages/database && bun run src/seed.ts",
    "db:setup": "bun run db:migrate && bun run db:seed",
    "db:studio": "cd packages/database && bun x prisma studio"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "typescript": "^5.7.3"
  }
}
